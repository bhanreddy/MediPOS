/** Session key: user signup / cloud subscription (Supabase Edge create-order). */
export const PHONEPE_SESSION_PENDING_USER = 'medpos_phonepe_merchant_order';

/** Session key: clinic owner billing upgrade (Express `/subscriptions/create`). */
export const PHONEPE_SESSION_PENDING_CLINIC = 'medpos_clinic_phonepe_merchant_order';

/** Session key: last payment verification error message for PaymentScreen. */
export const PHONEPE_SESSION_PAYMENT_ERROR = 'medpos_payment_error';
